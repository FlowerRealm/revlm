---
description: 嵌套 Store：实体只描述形态，Store 是 odb::database 上的薄 repository
globs: backend/**
alwaysApply: false
---

# 嵌套 Store 数据结构

数据按 **Store 挂 Store** 组织：上层 Store 持有下层 Store，共享同一个 `odb::database &`。

当前层级：`UserStore` → `TokenStore` → `RequestStore`。

## Class 与 Store 职责分离

- **class**：只描述「长什么样」（字段 + ODB `#pragma db`），不持有/管理子结构集合。
- **Store**：无状态 repository；构造时接收 `odb::database &`，只做 CRUD / 查询，不缓存实体列表。
- **不要**为数据 Store 做单例、内存 `vector` 缓存、`reload` / `align_*_to_db`。

```
// ❌ BAD：实体 class 里直接塞子结构 vector / 管理逻辑
class Foo {
  long long id;
  std::vector<Bar> bars;  // 子结构不应挂在 class 上
};

// ❌ BAD：内存优先 + 整表对齐
class FooStore {
  static FooStore &instance();
  std::vector<Foo> foos_;
  void reload(odb::database &db);
  bool align_foo_to_db(size_t index);
};

// ✅ GOOD：class 只留形态；Store 薄 repository + 嵌套子 Store
class Foo {
#pragma db id auto
  long long id = 0;
  std::string name;
};
class FooStore {
public:
  explicit FooStore(odb::database &db);
  BarStore &bars();
  long long create(Foo foo);
  Foo get(long long id);
private:
  odb::database &db_;
  BarStore bars_;
};
```

## 读写流程

外来请求直接通过 Store 读写数据库（ODB `persist` / `update` / `erase` / `query`）。需要事务时在调用方用 `odb::transaction`。

```
// ❌ BAD：先改内存再对齐
foos_[i].name = name;
align_foo_to_db(i);

// ✅ GOOD：构造/取得实体后经 Store 落库
Foo foo = store.get(id);
foo.name = name;
store.update(foo);
```

## 启动

进程启动时调用 `ensure_schema(db)`（ODB embedded schema），**不要**手写 migration runner，也不要在启动时 `reload` 灌内存。

## 速查

| 职责 | 放哪里 |
|------|--------|
| 字段形态 + ODB 注解 | `class Entity` |
| CRUD + 子 Store 挂载 | `EntityStore(odb::database &)` |
| Schema | `ensure_schema` / ODB embedded DDL |
| 改数据 | Store 直接写库 |
| 嵌套 | 上层 Store 成员持有下层 Store |
