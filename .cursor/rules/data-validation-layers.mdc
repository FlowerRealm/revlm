---
description: 前后端与 store 层的数据校验分层约定
alwaysApply: true
---

# 数据校验分层

校验只发生在「边界」；越靠近持久化，越不校验。

## 前端（初步校验）

- 做格式与必填等即时反馈：邮箱格式、金额小数位、非空等。
- 目的是 UX，**不能**当作安全边界。

## 后端业务 / API 层（权威校验）

- **重验前端已做的规则**：客户端可被篡改，格式与约束必须再验一遍。
- **做需查库或跨实体的校验**：余额是否足够、用户是否存在、权限与配额等。
- 校验通过后再把已规范化的数据交给下层。

```
// ❌ BAD：信任前端传来的 email / amount，直接写库
// ✅ GOOD：API 层 normalize + 格式校验，再查余额/配额，通过后才调 store
```

## Store / 数据库交接层（禁止校验）

- `store/` 及同等「与 DB 交接」的管理层：**不再校验**业务数据。
- 不根据外界入参做格式/业务判断，也不为「再确认一遍」去读库做校验。
- **默认接受**上层已校验、已规范化的数据，专注读写与事务。

```
// ❌ BAD：在 store 里再验 email 格式、再查余额是否够扣
// ✅ GOOD：store 只执行 INSERT/UPDATE，假定调用方已保证合法性
```

## 边界速查

| 层 | 格式/必填 | 查库业务规则 |
|----|-----------|--------------|
| 前端 | 做 | 不做（或仅展示用） |
| 后端 API / 业务 | 重做 | 做 |
| store / DB 交接 | 不做 | 不做 |
