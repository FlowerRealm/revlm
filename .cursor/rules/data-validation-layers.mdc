---
description: 前后端与 store 层的数据校验分层约定（admin 跳过 / user 必验）
alwaysApply: true
---

# 数据校验分层

校验只发生在「边界」；越靠近持久化，越不校验。

## Admin vs User（核心）

按**谁能打这条数据**决定是否校验，而不是按路径里有没有 `admin` 字样：

| 入口 | 校验 |
| --- | --- |
| **Admin 侧**（仅管理员可写的数据，如 channel / channel_group 管理） | **跳过**：不做格式/必填/业务规则校验，解析后直接交给下层 |
| **User 侧**（普通用户也能打的数据，含用户自有资源与公开写接口） | **必须校验**：格式、必填、查库业务规则 |

```text
// ❌ BAD：admin 创建 channel 时再验 name 非空、type 枚举、base_url 格式
// ✅ GOOD：admin API 解析 JSON → 规范化必要字段 → 调 store；user API 则 normalize + 全量校验后再调 store
```

- Admin 仍可做**认证/鉴权**（是否管理员）与**结构性解析**（JSON 能否 parse、字段类型能否读出）；这些不是业务数据校验。
- 同一实体若既有 admin 写口又有 user 写口：admin 口跳过校验，user 口必须校验。

## 前端（初步校验）

- 做格式与必填等即时反馈：邮箱格式、金额小数位、非空等。
- 目的是 UX，**不能**当作安全边界。
- Admin 管理台前端可保留 UX 校验；后端 admin API **不**依赖、也**不**重做这些校验。

## 后端业务 / API 层（权威校验）

- **仅 User 侧**：重验前端已做的规则；做需查库或跨实体的校验（余额、存在性、权限与配额等）；通过后再把已规范化数据交给下层。
- **Admin 侧**：跳过上述业务/格式校验，信任已鉴权的管理员输入。

```text
// ❌ BAD：信任 user 传来的 email / amount，直接写库
// ✅ GOOD（user）：API 层 normalize + 格式校验，再查余额/配额，通过后才调 store
// ✅ GOOD（admin）：鉴权通过后 parse → 交 store，不做字段合法性校验
```

## Store / 数据库交接层（禁止校验）

- `store/` 及同等「与 DB 交接」的管理层：**不再校验**业务数据。
- 不根据外界入参做格式/业务判断，也不为「再确认一遍」去读库做校验。
- **默认接受**上层已处理过的数据，专注读写与事务。

```text
// ❌ BAD：在 store 里再验 email 格式、再查余额是否够扣
// ✅ GOOD：store 只执行 INSERT/UPDATE，假定调用方已保证该保证的部分
```

## 边界速查

| 层 | 格式/必填 | 查库业务规则 |
| --- | --- | --- |
| 前端（user / admin UX） | 做（UX） | 不做（或仅展示用） |
| 后端 User API / 业务 | 重做 | 做 |
| 后端 Admin API | 不做 | 不做 |
| store / DB 交接 | 不做 | 不做 |
