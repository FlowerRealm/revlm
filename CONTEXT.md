# Revlm 插件与渠道路由上下文

本上下文定义 Revlm 插件、渠道路由、模型目录和计费边界的共同语言。

## 插件领域

**插件**：由用户安装并完全信任的原生扩展，同时可以提供后端协议实现和前端界面。
_避免_：安全沙箱、能力白名单、依赖包

**插件 v3**：Revlm 当前整套插件产品架构的代号；它使用当前 `.revlm-plugin` 包契约，但包契约自身的格式版本仍称 v1。
_避免_：把插件 v3 等同于插件包格式 v3

**插件 ID**：插件包的稳定身份，大小写敏感；改变 ID（包括大小写变化）表示另一个插件，不表示升级。

**插件版本**：插件用于展示和记录的版本字符串。它不决定是否兼容，也不表示宿主可以并存多个版本；同 ID 更新的冷启动迁移失败时，宿主恢复更新前的旧包和旧启用状态。

**插件清单**：插件对外提供的最小描述，包含 ID、名称、介绍和版本。

**插件类别**：插件清单中的固定枚举类别字段。v3 当前唯一合法值是 `channel`，表示插件参与渠道协议数据面；未知类别值不属于有效 v3 包。插件类别与 ChannelGroup.type 不是同一个概念。

**插件根目录**：一个已安装插件的唯一代码与资源边界。插件根目录不是持久化业务数据的归属地。

**启用插件**：在下一次 Revlm 冷启动时参与后端加载、前端加载和数据库迁移的插件。

**禁用插件**：不参与正常请求处理、前端加载、普通端点注册或数据库迁移，也不进入运行时的 `LD_PRELOAD`；重新启用后等下一次冷启动才生效。

**插件前端**：插件提供的前端入口和静态资源。核心控制台动态加载插件入口；任一插件入口语法错误、运行时异常或资源加载失败都会阻止控制台启动。

**插件异常**：插件 hook 或普通全局 HTTP 端点抛出未捕获异常时，宿主在 HTTP 边界捕获并让当前请求返回 HTTP 500；不自动调用提交函数、不扣费，worker 继续服务。

**插件后端**：插件提供的原生协议处理、上游转换、流式处理、协议计费语义和插件数据库逻辑。

## 渠道与请求

**用户 API key**：用户调用 Revlm 时携带的凭据。它归属于一个 ChannelGroup，并以此决定请求上下文。

**Channel**：一个具体的上游地址、凭据和路由优先级。Channel 不拥有协议类型、模型目录或插件配置。

**ChannelGroup**：用户 API key 归属的渠道集合，也是一次请求的路由归属单位。

**ChannelGroup.type**：ChannelGroup 使用的上游协议类型。它属于 ChannelGroup，不属于 Channel，也不是插件 ID。

**plugin.type**：插件自身的类别。v3 当前唯一合法值是 `channel`，表示插件参与渠道协议数据面；它不描述某个 ChannelGroup 的具体协议。

**插件协议映射**：`channel` 插件支持哪些 `ChannelGroup.type` 由插件代码直接判断；清单、核心 registry 和数据库不记录映射。多个插件命中同一 type 时不做冲突检测，直接遵循动态链接器的符号优先级；未命中的实现沿 `RTLD_NEXT` 链继续。

**协议请求**：需要经过用户 API key 鉴权并转发到上游的请求。请求路径只是协议输入，不负责选择插件。

**协议分发**：核心先解析用户 API key 的归属 ChannelGroup，再把请求交给插件处理的过程。

**ChannelGroup 路由上下文**：一次请求经过 API key 鉴权后确定的具体 ChannelGroup 及其协议、成员 Channel 和核心倍率。一个 channel 插件可以复用处理多个不同的 ChannelGroup。

**插件端点注册**：插件在启动阶段通过 `revlm_register_http_routes(Server&)` 一次性注册普通全局 Server 端点。它不是请求级插件发现或分流机制；普通路由冲突直接接受 `httplib::Server` 的注册顺序语义，宿主不检测。

**插件 hook**：核心 hpp 暴露的普通函数入口。插件通过提供同名 cpp 实现来补充未实现函数、覆盖已实现函数，或通过 `RTLD_NEXT` 链接下一层实现。核心只调用一个符号；各插件依据已经解析好的 ChannelGroup 上下文决定自己处理还是继续转发，宿主不维护插件对象注册表，也不逐个试探插件。

**同名 hook 链**：多个启用插件对同一个 hpp 函数提供同名实现形成的 `RTLD_NEXT` 链。每个插件都会进入一次，但非目标插件只检查 ChannelGroup.type 后转发；真正处理请求的插件不再转发。它是符号链，不是 C++ overload，也不是宿主维护的候选插件列表。

**无匹配协议插件**：已解析的 `ChannelGroup.type` 没有任何启用插件处理时，核心返回 HTTP 500；请求未进入协议插件处理，不调用提交函数，也不写入核心请求记录。

**完整协议 hook**：`channel` 插件通过同名 hpp/cpp hook 接管一次 `/v1` 协议请求的完整协议生命周期，包括请求解析、协议端点分支（含 `/v1/models`）、上游请求构造、响应或 SSE 解析、原始 usage 提取和协议计费语义处理。usage 的最终事件识别、协议字段路径、多事件合并和从 token 得出协议计费结果的规则都属于插件；核心只提供鉴权后的上下文、通用传输、最终扣费、倍率和核心记录能力，不通过 `Gateway` 对象和虚函数逐步驱动插件。

**协议 hook ABI**：`revlm_handle_v1(const httplib::Request &, httplib::Response &, ProxyRequest &)`。它直接复用现有 HTTP 类型和可变核心请求记录；不额外引入 RequestContext、ResponseWriter 或函数表抽象。插件与当前 Revlm/httplib ABI 一起升级，并通过冷启动生效。

**选中 Channel**：核心根据 ChannelGroup 状态、成员 Channel 状态、优先级和轮询规则选出的本次上游目标。协议 hook 接收这个目标，只负责协议处理；插件不通过 `Channel.type` 或插件数据库判断 Channel 是否可用。

**协议级失败切换**：一次上游尝试失败后是否切换到 ChannelGroup 的下一个 Channel，由完整协议 hook 根据上游协议语义判断是否可恢复。网络错误、timeout 和可恢复的上游 5xx 可以继续轮转；确定性的鉴权错误、请求参数错误或协议不支持等错误由插件终止请求。候选序列走到末尾后回到第一个 Channel 继续尝试，不设置重试次数上限，也不因完整一轮失败就返回错误。每次尝试仍受单次上游 timeout 约束；只有上游成功、客户端断开、请求取消、不可恢复错误或 Revlm 关闭时才终止。核心只提供按既定顺序取得候选 Channel 的能力，不根据通用 HTTP 状态码替插件做重试，也不在流已经开始后切换。

**请求提交**：完整协议 hook 在上游成功、客户端断开、请求取消、不可恢复错误或 Revlm 关闭等最终结果确定后调用核心提交函数。一次客户端请求无论循环尝试多少个 Channel，都只提交一次核心请求记录；中间 Channel 尝试不生成独立记录，也不独立计费。插件必须自行保证不重复调用提交函数；核心不做幂等保护，重复调用可能重复落库和扣费。插件在最终提交前按协议确定最终 usage，填入原始 `token_details` 和运行时基础金额 `protocol_cost_usd`；核心负责应用 ChannelGroup 倍率、最终扣款并保存核心请求记录。最终数据库只保存当次 `ChannelGroupMultiplier` 和 `usd`，不单独保存 `protocol_cost_usd`；基础金额可以根据两者反推。没有 usage 或因中断终止的请求不扣费，运行时 `protocol_cost_usd` 和最终 `usd` 都为 0，已捕获的状态、延迟和错误信息仍然保留。核心不扫描或猜测协议的 usage 事件，不解析协议 token JSON，也不从 hook 返回时机猜测流是否完成。若提交扣款或写库失败，响应尚未写出时返回 HTTP 500；HTTP/SSE 已开始发送时不能改写，只结束连接，不重试上游。

**`/v1` 特殊入口**：核心拥有的唯一协议数据面入口。核心先完成 API key 到具体 ChannelGroup 的解析，再调用同名 hook 链；插件不能把相同 `/v1` 路径无条件注册到共享全局 Server。

## 数据与计费

**核心请求记录**：Revlm 为审计、路由追踪和计费保存的客户端请求级通用记录，不是上游 Channel 尝试记录。一次客户端请求只产生一条核心请求记录，不以 usage 是否存在或中间尝试次数作为创建条件。成功时 `channel_id` 是成功的 Channel；请求因客户端断开、取消、不可恢复错误或 Revlm 关闭终止时，`channel_id` 是最后一次实际尝试的 Channel。插件提供完整原始 `token_details` 和运行时基础金额 `protocol_cost_usd`，核心负责应用核心倍率、完成最终扣费并持久化 `ChannelGroupMultiplier` 与最终 `usd`；请求表不保存 `tier_multiplier`、`service_tier` 等协议计费字段，无 usage 或流式中断时基础金额按零处理。

**插件拥有的数据**：某个插件理解并负责迁移、读写、清理的协议配置和插件专属业务表数据；核心请求记录及其计费写入不属于插件。

**协议计费结果**：插件根据协议原始 token/usage JSON、模型和协议自身的计费规则得到的运行时基础金额 `protocol_cost_usd`。协议专属的计费维度只在插件内部处理，不被核心固定成 input/output/cache 字段；插件通过核心提交接口把基础金额交给核心，但核心不单独持久化它。核心不解析协议 JSON，只负责应用 ChannelGroup 倍率、扣款和核心记录写入。

**token_details**：核心请求明细中的原始 token/usage JSON。它完整保留上游响应中的 token 字段，包括核心当前不认识的字段。普通响应直接保存最终 usage 对象；流式响应由插件按协议识别最终事件，必要时在插件内部合并多个 usage 事件后保存。核心不按 `ChannelGroup.type` 解析 JSON、不递归扫描 SSE，也不合并协议事件；核心只聚合请求数、最终金额和首 token 延迟等通用指标，协议 token 统计由插件或原始 JSON 承担。

**ChannelGroupMultiplier**：核心在请求提交时从 ChannelGroup 取得并写入请求记录的倍率快照。它与最终 `usd` 一起保存，用于审计和反推出运行时基础金额。

**最终金额**：核心接收插件产生的运行时 `protocol_cost_usd`，应用并保存 `ChannelGroupMultiplier` 后得到用于扣费和保存的 `usd`。

**模型**：由插件管理的模型目录项。共享模型结构只保留 `id`、`name` 和插件自定义的 `pricing` JSON；不包含固定的 input/output/cache 价格字段，也不包含 `owned_by` 或 `icon_url`。

**模型目录**：由插件按 ChannelGroup.type 提供并管理的模型和价格信息。不同插件可以有不同的 `pricing` JSON 结构；`Model.id` 只在插件自己的 ChannelGroup.type 范围内有效，核心不要求跨插件全局唯一。核心只按 ChannelGroup.type 索引和转发，不拥有模型目录，也不解析价格 JSON。

**插件迁移**：启用插件在核心数据库 schema 完成后执行的幂等数据库逻辑。`revlm_plugin_migrate()` 是可选的 `extern "C" void` 符号，缺失时按 no-op，正常返回表示成功、抛出 C++ 异常表示失败；插件通过核心公开的全局数据库访问符号操作自己的表；每次插件迁移在单个数据库事务中执行，失败自动回滚本次迁移；启用插件迁移失败会阻止 worker 启动，不带着半套插件 schema 运行。

**插件卸载清理**：卸载请求只标记 pending；下一次冷启动时，宿主在启动 worker 前通过可选的 `extern "C" void` `revlm_plugin_cleanup()` 执行插件自行决定的数据库清理，正常返回表示成功、抛出 C++ 异常表示失败，符号缺失时按 no-op；宿主不执行通用 down migration。清理成功后宿主才删除插件包；失败不自动重试，用户再次显式卸载时才重试 cleanup。

**冷启动**：外部进程重启 Revlm 后，宿主重新确定插件集合、执行生命周期并启动 worker 的过程。
