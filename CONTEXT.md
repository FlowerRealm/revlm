# Revlm 插件与渠道路由上下文

本上下文定义 Revlm 插件、渠道路由、模型目录和计费边界的共同语言。

## Language

### 插件领域

**插件**：由用户安装并完全信任的原生扩展，同时可以提供后端协议实现和前端界面。宿主在启动时加载插件动态库并调用它的注册入口，插件把自己的能力登记到核心注册表；插件不覆盖核心已有实现，也不依赖动态链接器的符号优先级。
_避免_：安全沙箱、能力白名单、依赖包、符号插桩、`LD_PRELOAD`

**插件 v3**：Revlm 当前整套插件产品架构的代号；它使用当前 `.revlm-plugin` 包契约，但包契约自身的格式版本仍称 v1。
_避免_：把插件 v3 等同于插件包格式 v3

**插件 ID**：插件包的稳定身份，大小写敏感；改变 ID（包括大小写变化）表示另一个插件，不表示升级。

**插件版本**：插件用于展示和记录的版本字符串。它不决定是否兼容，也不表示宿主可以并存多个版本。同 ID 更新直接替换旧包，宿主不保留旧包备份，也不在任何情况下恢复到更新前的包。
_避免_：版本回滚、备份包、版本兼容判断

**插件清单**：插件对外提供的最小描述，包含 ID、类别、名称、介绍、版本和控制面 ABI 版本。清单只描述插件自身，不包含宿主运行时才知道的目录、状态或错误信息。

**控制面 ABI 版本**：插件清单声明的整数，必须与宿主当前的控制面 ABI 版本相等，否则该插件成为失败插件。它保护的是注册、模型目录和生命周期这一层的 C 接口，不保护协议处理器。

**插件类别**：插件清单中的固定枚举类别字段。v3 当前唯一合法值是 `channel`，表示插件参与渠道协议数据面；未知类别值不属于有效 v3 包。插件类别与 ChannelGroup.type 不是同一个概念。

**插件根目录**：一个已安装插件的唯一代码与资源边界。插件根目录不是持久化业务数据的归属地，宿主也不在其中写入运行时状态。

**启用插件**：参与后端加载、注册、前端加载和数据库迁移的插件。启用与禁用即时生效于注册表，因此路由和模型目录立即变化；数据库迁移只在冷启动执行，所以新启用插件的迁移等下一次冷启动。

**禁用插件**：不参与请求处理、前端加载、端点注册或数据库迁移的插件。禁用即时从注册表注销，正在进行的请求不受影响。禁用清单保存在核心配置中，不使用文件系统标记。

**失败插件**：加载失败、控制面 ABI 版本不符、注册冲突、清单缺键或冷启动迁移失败的插件。它被记录为失败并按禁用处理，不进入注册表、不加载前端、不参与请求；其余插件和服务正常启动。失败不自动重试，用户重新上传该插件后重新尝试。

**插件前端**：插件提供的前端入口和静态资源。核心控制台动态加载插件入口；任一插件入口语法错误、运行时异常或资源加载失败都会阻止控制台启动。

**插件异常**：插件协议处理器或普通全局 HTTP 端点抛出未捕获异常时，宿主在 HTTP 边界捕获并让当前请求返回 HTTP 500；不自动调用提交函数、不扣费，服务继续。

**插件后端**：插件提供的原生协议处理、上游转换、流式处理、协议计费语义和插件数据库逻辑。

**冷启动**：外部进程重启 Revlm 后，宿主重新确定插件集合、执行生命周期并开始服务的过程。

### 渠道与请求

**用户 API key**：用户调用 Revlm 时携带的凭据。它归属于一个 ChannelGroup，并以此决定请求上下文。

**Channel**：一个具体的上游地址、凭据和路由优先级。Channel 不拥有协议类型、模型目录或插件配置。同一个协议插件通过不同 Channel 服务不同供应商：供应商差异是数据，不是代码。

**ChannelGroup**：用户 API key 归属的渠道集合，也是一次请求的路由归属单位。

**ChannelGroup.type**：ChannelGroup 使用的上游协议类型，自由字符串。核心不校验它的取值、不维护合法值清单；新增协议只需要新增插件和使用该 type 的 ChannelGroup，核心无需改动。它属于 ChannelGroup，不属于 Channel，也不是插件 ID。

**plugin.type**：插件自身的类别。v3 当前唯一合法值是 `channel`；它不描述某个 ChannelGroup 的具体协议。

**路由表**：核心持有的数据面注册表，键是方法、路径和 ChannelGroup.type 的组合，值是插件提供的协议处理器。表由插件在注册阶段填写，核心不预置任何条目，因此核心不认识任何协议名称和路径前缀。键有两种：普通路径只匹配自身；以 `/` 结尾的路径同时认领它下面的一切，按最长前缀命中，用于 `GET /v1/models/` 这类子资源端点。核心仍然不解析路径——尾部是模型 ID 还是别的东西由插件自己从请求路径读取。

**注册冲突**：两个启用插件向路由表登记同一个键。宿主在注册阶段直接报错并让后注册的插件成为失败插件，不按加载顺序静默取舍。

**插件协议映射**：一个插件支持哪些 `ChannelGroup.type` 由它注册了哪些路由表条目决定；清单和数据库不记录映射。

**插件注册**：插件在加载阶段通过注册入口一次性登记自己的能力，包括数据面路由、模型目录和普通全局 HTTP 端点。它不是请求级发现或分流机制。普通全局端点的路径冲突接受 `httplib::Server` 的注册顺序语义，宿主不检测；数据面路由的冲突按注册冲突处理。

**协议请求**：需要经过用户 API key 鉴权并转发到上游的请求。请求路径只是路由表的一部分键，不单独决定插件。

**协议分发**：核心解析用户 API key 的归属 ChannelGroup，用方法、路径和该组的 type 查路由表，调用唯一命中的协议处理器的过程。核心只调用一个处理器，不遍历插件、不逐个试探、不依赖转发链。

**ChannelGroup 路由上下文**：一次请求经过 API key 鉴权后确定的具体 ChannelGroup 及其协议、成员 Channel 和核心倍率。一个插件可以注册多个 type 的路由并复用同一份实现。

**协议处理器**：插件登记到路由表的函数，处理**一次上游尝试**，包括请求解析、协议端点分支（含模型列表端点）、上游请求构造、响应或 SSE 解析、原始 usage 提取和协议计费语义处理。usage 的最终事件识别、协议字段路径、多事件合并和从 token 得出协议计费结果的规则都属于插件。处理器返回**尝试裁决**，核心据此决定终止或换下一个候选；核心不通过对象和虚函数逐步驱动插件。

**尝试裁决**：协议处理器每次返回的两个值之一。`Done` 表示最终结果已确定（上游成功，或不可恢复的错误），`NextCandidate` 表示这次失败可恢复、应当换下一个候选 Channel。可恢复性由插件按上游协议语义判断；核心不读状态码也不读响应体（[ADR 0009](docs/adr/0009-core-owned-failover-loop.md)）。

**核心服务面**：核心提供给协议处理器调用的最小函数集合，包括上游传输与流式响应交接。候选 Channel 的轮转与请求提交不在其中——它们是核心循环自身的行为，插件既不调用也无法调用。鉴权、路由、SSRF 校验、余额预检、倍率和扣款留在核心；SSE 分帧与 usage 解析属于插件。

**无匹配协议插件**：已解析的方法、路径和 `ChannelGroup.type` 在路由表中没有条目时，核心返回 HTTP 500；请求未进入插件处理，不调用提交函数，也不写入核心请求记录。

**选中 Channel**：核心根据 ChannelGroup 状态、成员 Channel 状态、优先级和轮询规则选出的本次上游目标。协议处理器接收这个目标，只负责协议处理；插件不判断 Channel 的通用可用性。

**协议级失败切换**：核心持有的候选轮转循环。每轮取一个候选 Channel、调用一次协议处理器、按返回的尝试裁决决定继续或退出。是否可恢复由插件判断——网络错误、timeout 和可恢复的上游 5xx 返回 `NextCandidate`；确定性的鉴权错误、请求参数错误或协议不支持返回 `Done`。轮转顺序、走到末尾后回到第一个 Channel、每个候选各试一次后停止、每次尝试各自受单次上游 timeout 约束，这些都由核心实现一次，不由插件重复。轮转是环形的，所以循环必须有下界：组内每个 Channel 都尝试过一轮之后核心停止轮转，返回 HTTP 502，并把最后一次的错误信息带上——全部上游都不可用时，客户端最需要的是尽快得到答复，而不是被无限重试挂住。除此之外只有上游成功、客户端断开、请求取消、不可恢复错误或 Revlm 关闭时才终止。核心不根据通用 HTTP 状态码替插件做重试。处理器一旦把流交接给核心就不再切换：此时返回 `NextCandidate` 由核心按 `Done` 处理并记为插件错误，而不是把半个响应接到另一个上游的输出后面。

**流式响应交接**：协议处理器不同步向客户端写字节——HTTP 服务器只在请求处理函数返回后才交出可写 sink。处理器改为把状态码、响应头和一个响应体 pump 交接给核心，核心装载它并在 pump 结束时提交。SSE 分帧、usage 提取和空闲超时都在 pump 内部，核心依然不解析。这个时序同时就是「流开始后不切换」的机制：处理器还在决定是否换候选时拿不到 sink。核心交给 pump 的客户端 sink 除了写入还提供存活探测：只靠写入无法发现一个在上游沉默期间离开的客户端，pump 因此在等待上游的间隙探测客户端，一旦客户端离开就切换到短暂的收尾窗口，把上游还能送达的最终 usage 收完就结束，而不是占着上游连接等到空闲超时。

**请求提交**：核心在失败切换循环退出后执行一次，位置是循环的唯一出口；交接过流的请求把这一次提交推迟到 pump 结束，仍然是一次退出、一次提交。一次客户端请求无论轮转多少个 Channel，都只提交一次核心请求记录；中间 Channel 尝试不生成独立记录，也不独立计费。插件没有调用提交的接口，因此不存在重复提交，核心也不需要幂等保护。插件在返回 `Done` 之前按协议确定最终 usage，填入请求上下文的原始 `usage_details` 和运行时基础金额 `protocol_cost_usd`；核心负责应用 ChannelGroup 倍率、最终扣款并保存核心请求记录。最终数据库只保存当次 `ChannelGroupMultiplier` 和 `usd`，不单独保存 `protocol_cost_usd`；基础金额可以根据两者反推。没有 usage 或因中断终止的请求不扣费，运行时 `protocol_cost_usd` 和最终 `usd` 都为 0，已捕获的状态、延迟和错误信息仍然保留。核心不扫描或猜测协议的 usage 事件，不解析协议 token JSON，也不从处理器返回时机猜测流是否完成。若提交扣款或写库失败，响应尚未写出时返回 HTTP 500；HTTP/SSE 已开始发送时不能改写，只结束连接，不重试上游。核心请求记录只在请求真正到达过某个 Channel 时产生：没有可用 Channel 或没有匹配路由的请求不落库，也不计费。

### 数据与计费

**核心请求记录**：Revlm 为审计、路由追踪和计费保存的客户端请求级通用记录，不是上游 Channel 尝试记录。一次客户端请求只产生一条核心请求记录，不以 usage 是否存在或中间尝试次数作为创建条件。成功时 `channel_id` 是成功的 Channel；请求因客户端断开、取消、不可恢复错误或 Revlm 关闭终止时，`channel_id` 是最后一次实际尝试的 Channel。插件提供完整原始 `usage_details` 和运行时基础金额 `protocol_cost_usd`，核心负责应用核心倍率、完成最终扣费并持久化 `ChannelGroupMultiplier` 与最终 `usd`。

**核心记录维度**：核心请求记录中拥有独立列的字段，判定标准是核心是否按它聚合或过滤，而不是每个协议是否都有这个概念。当前维度包括模型名、状态码、延迟、首 token 延迟、上游关联标识、最终金额和倍率。协议专属的一切进入 `usage_details`，不新增列。不适用该维度的协议留空或留零，这是不适用，不是冲突。

**插件拥有的数据**：某个插件理解并负责迁移、读写、清理的协议配置和插件专属业务表数据；核心请求记录及其计费写入不属于插件。

**协议计费结果**：插件根据协议原始 token/usage JSON、模型和协议自身的计费规则得到的运行时基础金额 `protocol_cost_usd`。协议专属的计费维度只在插件内部处理，不被核心固定成 input/output/cache 字段；插件通过核心提交接口把基础金额交给核心，但核心不单独持久化它。核心不解析协议 JSON，只负责应用 ChannelGroup 倍率、扣款和核心记录写入。

**usage_details**：核心请求明细中的原始 usage JSON。它完整保留上游响应中的计量字段，包括核心当前不认识的字段。不同协议的计量单位不同——token、图片张数、音频秒数都放在这里，因此它不以 token 命名。普通响应直接保存最终 usage 对象；流式响应由插件按协议识别最终事件，必要时在插件内部合并多个 usage 事件后保存。核心不按 `ChannelGroup.type` 解析 JSON、不递归扫描 SSE，也不合并协议事件；核心只聚合请求数、最终金额和首 token 延迟等通用指标。

**ChannelGroupMultiplier**：核心在请求提交时从 ChannelGroup 取得并写入请求记录的倍率快照。它与最终 `usd` 一起保存，用于审计和反推出运行时基础金额。

**最终金额**：核心接收插件产生的运行时 `protocol_cost_usd`，应用并保存 `ChannelGroupMultiplier` 后得到用于扣费和保存的 `usd`。

**模型**：由插件管理的模型目录项。共享模型结构只保留 `id`、`name` 和插件自定义的 `pricing` JSON；不包含固定的 input/output/cache 价格字段，也不包含 `owned_by` 或 `icon_url`。

**模型目录**：由插件在注册阶段按 ChannelGroup.type 登记并管理的模型和价格信息。不同插件可以有不同的 `pricing` JSON 结构；`Model.id` 只在插件自己的 ChannelGroup.type 范围内有效，核心不要求跨插件全局唯一。核心只按 ChannelGroup.type 索引和转发，不拥有模型目录，也不解析价格 JSON。

**插件迁移**：启用插件在核心数据库 schema 完成后执行的幂等数据库逻辑。宿主在冷启动时逐个加载插件并调用它的迁移入口，缺失时按 no-op，正常返回表示成功、抛出异常表示失败；插件通过核心公开的数据库访问接口操作自己的表；每次插件迁移在单个数据库事务中执行，失败自动回滚本次迁移，因此不存在半套插件 schema。迁移失败的插件成为失败插件，其余插件和服务正常启动。

**插件卸载清理**：卸载请求只标记 pending；下一次冷启动时，宿主在开始服务前加载待卸载插件并调用它的清理入口，执行插件自行决定的数据库清理，正常返回表示成功、抛出异常表示失败，入口缺失时按 no-op；宿主不执行通用 down migration。清理成功后宿主才删除插件包；失败不自动重试，用户再次显式卸载时才重试清理。
